# Manifests

This directory contains a copied original run manifest for the deterministic
`fbt_atlas_v0` smoke run.

The copied manifest retains the original absolute paths from the run environment. The
archive-local files are verified by `release/stage4-p4-draft/checksums.txt`.
