# Reproduction Commands

Run from the repository root.

## Environment Check

```bash
py -m uv run fts doctor --release-check
```

## Test Suite

```bash
py -m uv run ruff check .
py -m uv run ruff format --check .
py -m uv run mypy src
py -m uv run pytest
```

## Regenerate Stage 2 Artifact

```bash
py -m uv run fts fbt reproduce-numerical-example
py -m uv run fts validate-manifest experiments/manifests/<fbt-numerical-manifest>.json
```

The capsule artifacts were generated by:

```bash
py -m uv run fts fbt reproduce-numerical-example
py -m uv run fts validate-manifest experiments/manifests/ART-TASK-002-FBT-NUMERICAL-MANIFEST-20260625T222000Z-2A277325.json
```

## Expected Checksums For Generated Outputs

```text
raw source table: 2cba53cb7a7b5aa01e4c7b6a6f13b334b708f90419612f3c2eb82062b481a1cd
derived exact JSON: b5dabb660739de45d4d943e64b5195b7e85236cfe8ce5d0f2a3c61412abeb509
derived Markdown report: af7831b01b606eee35ced1734db1b86e93b230fd0a3262ac34a392880d07d565
```

Manifest timestamps, run IDs, absolute paths, and environment metadata may differ on
regeneration. The exact JSON/report checksums should remain stable if the source input
and implementation are unchanged.
